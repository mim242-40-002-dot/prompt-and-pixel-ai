document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.pv-toggle');const menu=document.querySelector('.pv-menu');if(toggle&&menu){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));menu.classList.toggle('open',!open)})}const filters=[...document.querySelectorAll('[data-alignment]')];const cards=[...document.querySelectorAll('[data-character]')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.setAttribute('aria-pressed',String(b===btn)));const value=btn.dataset.alignment;cards.forEach(card=>card.classList.toggle('hidden',value!=='all'&&card.dataset.character!==value))}));const dialog=document.querySelector('.pv-dialog');document.querySelectorAll('[data-profile]').forEach(btn=>btn.addEventListener('click',()=>{if(!dialog)return;dialog.querySelector('[data-dialog-title]').textContent=btn.dataset.name;dialog.querySelector('[data-dialog-copy]').textContent=btn.dataset.copy;dialog.showModal()}));dialog?.querySelector('.dialog-close')?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});document.querySelectorAll('[data-static-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const status=form.querySelector('.pv-status');if(status)status.textContent='Idea checked locally. This static academic demo does not transmit or store submissions.'}))});
